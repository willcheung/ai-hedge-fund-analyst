# SYNTHETIC interface fixture — not research

The schema is copied only from the checked-in dashboard contract, never a live wiki. Empty tickers intentionally exercises missing/degraded generation with no symbol lookup. Portability tests copy this tree to arbitrary paths and validate the strict mirror, output schema, and missing root failures. This is not a production wiki initializer or evidence that a real external schema is aligned.
