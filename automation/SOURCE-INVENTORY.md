# Public source structure

`source-inventory.json` lists relative Python and shell source paths only. Regenerate with `python3 automation/tools/inventory_sources.py`. It contains no deployment inventory, private import provenance or source-value fingerprints. See `capabilities.json` for disabled optional capability groups.
